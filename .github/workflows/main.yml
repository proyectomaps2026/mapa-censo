name: Actualizar CSV de Hacienda
on:
  workflow_dispatch: 

jobs:
  descarga:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Descargar y Convertir Datos
        env:
          AIRTABLE_API: ${{ secrets.AIRTABLE_API }}
        run: |
          pip install requests
          python - <<EOF
          import os, requests, csv
          
          token = os.environ.get("AIRTABLE_API")
          base_id = "appqzHunqskWZJNc2"
          table_id = "tblWiTUee1VWK9OZ2"
          
          url = f"https://api.airtable.com/v0/{base_id}/{table_id}"
          headers = {"Authorization": f"Bearer {token}"}
          
          print("Conectando con Airtable...")
          res = requests.get(url, headers=headers)
          data = res.json()
          
          if "records" in data:
              filas = [r["fields"] for r in data["records"]]
              if filas:
                  # Recolectamos TODAS las columnas posibles de todos los registros
                  columnas_totales = set()
                  for f in filas:
                      columnas_totales.update(f.keys())
                  
                  # Guardamos el CSV con todas las columnas encontradas
                  with open("datos_censo.csv", "w", newline="", encoding="utf-8") as f:
                      writer = csv.DictWriter(f, fieldnames=list(columnas_totales))
                      writer.writeheader()
                      writer.writerows(filas)
                  print("¡Archivo datos_censo.csv creado con éxito!")
              else:
                  print("La tabla está vacía.")
          else:
              print("Error de Airtable:", data)
          EOF

      - name: Guardar en GitHub
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add datos_censo.csv
          git commit -m "Sincronización Hacienda Final" || exit 0
          git push
